
<%= button_to 'Create New Room', { :action => :new, :method => :get }, :class => 'btn btn-primary' %>

<%= form_tag rooms_join_room_path, :method => :get, :id => 'join_room_form' %>
    <%= text_field :room_id, "Room ID", :id => 'room_id_input' %>
    <%= submit_tag 'Join a room', :id => 'join_room_button', :class => 'btn btn-primary' %>

