
<%= button_to 'Create New Room', new_room_path,  { :method => :get, :id => "create_room_button", :class => 'btn btn-primary' } %>

<%= form_tag rooms_join_room_path, :method => :get %>
    <%= text_field :room_id, "Room ID", :id => 'room_id_input' %>
    <%= submit_tag 'Join a room', :id => 'join_room_button', :class => 'btn btn-primary' %>

